<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        PTT Vendor List View
        Purpose: Easy-to-use vendor list for sales team to search and filter vendors
        All custom fields use ptt_ prefix (Odoo best practice).
    -->

    <!-- Vendor List/Tree View -->
    <record id="view_ptt_vendor_tree" model="ir.ui.view">
        <field name="name">res.partner.vendor.tree.ptt</field>
        <field name="model">res.partner</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <list string="Vendors" 
                  decoration-success="ptt_vendor_compliance_status == 'compliant'"
                  decoration-warning="ptt_vendor_compliance_status == 'expiring_soon'"
                  decoration-danger="ptt_vendor_compliance_status in ('missing_required', 'expired')">
                <field name="name" string="Vendor Name"/>
                <field name="ptt_vendor_tier" string="Tier" widget="badge"
                       decoration-success="ptt_vendor_tier == 'premier'"
                       decoration-info="ptt_vendor_tier == 'classic'"
                       decoration-muted="ptt_vendor_tier == 'essentials'"/>
                <field name="ptt_vendor_service_types" string="Services" widget="many2many_tags"/>
                <field name="ptt_vendor_compliance_status" string="Compliance" widget="badge"
                       decoration-success="ptt_vendor_compliance_status == 'compliant'"
                       decoration-warning="ptt_vendor_compliance_status == 'expiring_soon'"
                       decoration-danger="ptt_vendor_compliance_status in ('missing_required', 'expired')"/>
                <field name="ptt_vendor_rating" string="Rating" widget="badge"
                       decoration-success="ptt_vendor_rating in ('4', '5')"
                       decoration-warning="ptt_vendor_rating == '3'"
                       decoration-danger="ptt_vendor_rating in ('1', '2')"
                       optional="show"/>
                <field name="ptt_assignment_count" string="Jobs" optional="show"/>
                <field name="ptt_total_paid" string="Total Paid" widget="monetary" optional="show"/>
                <field name="phone" string="Phone"/>
                <field name="email" string="Email"/>
                <field name="ptt_vendor_notes" string="Notes" optional="hide" widget="text"/>
                <field name="ptt_vendor_document_count" string="Docs" optional="hide"/>
                <field name="city" optional="hide"/>
                <field name="state_id" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Vendor Kanban View -->
    <record id="view_ptt_vendor_kanban" model="ir.ui.view">
        <field name="name">res.partner.vendor.kanban.ptt</field>
        <field name="model">res.partner</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="name"/>
                <field name="ptt_vendor_tier"/>
                <field name="ptt_vendor_service_types"/>
                <field name="ptt_vendor_compliance_status"/>
                <field name="city"/>
                <field name="state_id"/>
                <field name="phone"/>
                <field name="email"/>
                <field name="image_128"/>
                <templates>
                    <t t-name="card">
                        <aside>
                            <img class="rounded" t-att-src="kanban_image('res.partner', 'image_128', record.id.raw_value)" alt="Vendor"/>
                        </aside>
                        <main>
                            <div class="d-flex align-items-center gap-1">
                                <field name="name" class="fw-bold fs-5 text-truncate"/>
                                <field name="ptt_vendor_tier" widget="badge"
                                       decoration-success="ptt_vendor_tier == 'premier'"
                                       decoration-info="ptt_vendor_tier == 'classic'"
                                       decoration-muted="ptt_vendor_tier == 'essentials'"/>
                            </div>
                            <field name="ptt_vendor_service_types" widget="many2many_tags"/>
                            <field name="ptt_vendor_compliance_status" widget="badge"
                                   decoration-success="ptt_vendor_compliance_status == 'compliant'"
                                   decoration-warning="ptt_vendor_compliance_status == 'expiring_soon'"
                                   decoration-danger="ptt_vendor_compliance_status in ('missing_required', 'expired')"/>
                            <div class="d-flex gap-2 mt-1">
                                <span t-if="record.phone.value" class="text-muted small">
                                    <i class="fa fa-phone"/> <t t-esc="record.phone.value"/>
                                </span>
                                <span t-if="record.email.value" class="text-muted small">
                                    <i class="fa fa-envelope"/> <t t-esc="record.email.value"/>
                                </span>
                            </div>
                            <div class="d-flex gap-2 mt-1" t-if="record.ptt_vendor_rating.value">
                                <span class="text-muted small">
                                    <i class="fa fa-star"/> <t t-esc="record.ptt_vendor_rating.value"/> Stars
                                </span>
                            </div>
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Vendor Search View -->
    <record id="view_ptt_vendor_search" model="ir.ui.view">
        <field name="name">res.partner.vendor.search.ptt</field>
        <field name="model">res.partner</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Vendor" 
                       filter_domain="['|', '|', ('name', 'ilike', self), ('email', 'ilike', self), ('phone', 'ilike', self)]"/>
                <field name="ptt_vendor_service_types" string="Service"/>
                <field name="phone"/>
                <field name="email"/>
                <field name="city" string="City" optional="hide"/>
                <field name="state_id" string="State" optional="hide"/>
                <separator/>
                <filter string="Compliant" name="compliant" domain="[('ptt_vendor_compliance_status', '=', 'compliant')]"/>
                <filter string="Missing Documents" name="missing_docs" domain="[('ptt_vendor_compliance_status', '=', 'missing_required')]"/>
                <filter string="Expired Documents" name="expired_docs" domain="[('ptt_vendor_compliance_status', '=', 'expired')]"/>
                <filter string="Expiring Soon" name="expiring_soon" domain="[('ptt_vendor_compliance_status', '=', 'expiring_soon')]"/>
                <separator/>
                <filter string="Premier Tier" name="tier_premier" domain="[('ptt_vendor_tier', '=', 'premier')]"/>
                <filter string="Classic Tier" name="tier_classic" domain="[('ptt_vendor_tier', '=', 'classic')]"/>
                <filter string="Essentials Tier" name="tier_essentials" domain="[('ptt_vendor_tier', '=', 'essentials')]"/>
                <filter string="No Tier Set" name="tier_none" domain="[('ptt_vendor_tier', '=', False)]"/>
                <separator/>
                <filter string="4-5 Stars" name="rating_high" domain="[('ptt_vendor_rating', 'in', ['4', '5'])]"/>
                <filter string="3 Stars" name="rating_medium" domain="[('ptt_vendor_rating', '=', '3')]"/>
                <filter string="1-2 Stars" name="rating_low" domain="[('ptt_vendor_rating', 'in', ['1', '2'])]"/>
            </search>
        </field>
    </record>

    <!-- Vendor List Action - ACTIVE VENDORS ONLY -->
    <record id="action_ptt_vendor_list" model="ir.actions.act_window">
        <field name="name">Vendors</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('supplier_rank', '>', 0), ('is_company', '=', True), ('ptt_vendor_status', '=', 'active')]</field>
        <field name="context">{
            'default_supplier_rank': 1,
            'default_is_company': True,
            'default_ptt_vendor_status': 'active'
        }</field>
        <field name="search_view_id" ref="view_ptt_vendor_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No vendors found
            </p>
            <p>
                Create a new vendor or adjust your search filters.
            </p>
        </field>
    </record>

    <!-- Bind custom views to the action -->
    <record id="action_ptt_vendor_list_tree" model="ir.actions.act_window.view">
        <field name="sequence">1</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_ptt_vendor_tree"/>
        <field name="act_window_id" ref="action_ptt_vendor_list"/>
    </record>

    <record id="action_ptt_vendor_list_kanban" model="ir.actions.act_window.view">
        <field name="sequence">2</field>
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="view_ptt_vendor_kanban"/>
        <field name="act_window_id" ref="action_ptt_vendor_list"/>
    </record>

    <!-- Menu item is in ptt_vendor_menus.xml -->

</odoo>
