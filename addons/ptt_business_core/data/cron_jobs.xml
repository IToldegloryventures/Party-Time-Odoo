<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!--
    PTT Event Reminder Cron Jobs
    ============================
    Scheduled actions that run daily to send event reminders
    to project managers for upcoming events.
    -->

    <!-- DISABLED - 10-Day Event Reminder Cron Job -->
    <record id="ptt_cron_event_reminder_10_day" model="ir.cron">
        <field name="name">PTT: 10-Day Event Reminder</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_event_reminders_10_day()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="False"/>
        <field name="nextcall" eval="(DateTime.now().replace(hour=8, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="priority">10</field>
    </record>

    <!-- DISABLED - 3-Day Urgent Event Reminder Cron Job -->
    <record id="ptt_cron_event_reminder_3_day" model="ir.cron">
        <field name="name">PTT: 3-Day Urgent Event Reminder</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_event_reminders_3_day()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="False"/>
        <field name="nextcall" eval="(DateTime.now().replace(hour=8, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="priority">5</field>
    </record>

</odoo>
