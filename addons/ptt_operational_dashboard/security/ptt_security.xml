<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- COO Security Group -->
    <record id="group_ptt_coo" model="res.groups">
        <field name="name">COO: Sales Commission Access</field>
        <field name="comment">Access to Sales Commission Reports - Chief Operating Officer only.</field>
    </record>

    <!-- Record Rules -->
    
    <!-- Personal To-Do: Users can only see their own items -->
    <record id="ptt_personal_todo_user_rule" model="ir.rule">
        <field name="name">Personal To-Do: User owns</field>
        <field name="model_id" ref="model_ptt_personal_todo"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>

